window.addEventListener("DOMContentLoaded", function(){
	function FncDatosUser(){
		const nombre= document.getElementById("TxtNombre").value;
	const apellido= document.getElementById("TxtApellido").value;
		const email= document.getElementById("email").value;
		const pais= document.getElementById("SltPais").value;
		const comentario= document.getElementById("TxtComentarios").value;
		const genero= document.querySelector("input[name="rdoGenero"]")?.value;
		const condiciones= document.getElementById("ChkTermino").checked;
		
		Validacion adicional manual:
		
		Mostrar en pantalla:
		
};